import React from 'react';
import AllEmployee from '../../core/components/fws/apply/employee/allEmployee';

export default function Employee({ navigation }: any) {
  return <AllEmployee navigation={navigation} />;
}
