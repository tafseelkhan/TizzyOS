import React from 'react';
import EmployeeDetail from '../../core/components/fws/apply/employee/employeeDetail';

export default function Employee({ navigation, route }: any) {
  return <EmployeeDetail navigation={navigation} route={route} />;
}
